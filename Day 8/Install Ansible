'''Task Recap

Install Ansible version 4.8.0

On the jump host (current server)

Use pip3 only

Make Ansible binary globally available (usable by all users)'''

#Update package index

sudo apt-get update

#Ensure Python 3 and pip3 are present

sudo apt-get install -y python3 python3-pip

#Install the specific Ansible version with pip3 (system-wide)

sudo pip3 install ansible==4.8.0

#Verify the installation

ansible --version
